{
  "name": "fenix-hub",
  "version": "0.3.8",
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "build:appimage": "./scripts/build-appimage.sh",
    "dev:vite": "bun run --cwd frontend dev",
    "build:vite": "bun run --cwd frontend build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.10.1"
  }
}
